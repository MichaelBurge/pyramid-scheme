(begin
  (defmacro (id exp) exp)
  (id 5)
  (defmacro (dup exp)
            `(begin
               ,exp
               ,exp))
  (dup 10)
  (defmacro (check-memory exp)
    (if (accesses-memory? exp)
        '5
        '10))
  (check-memory 'aoeu)
  )

